export { waterPassives } from "./water_passives";
export { windPassives } from "./wind_passives";

// export const passives = [waterPassives, windPassives]
//     .reduce((p, c) => Object.assign(p, c), {});
